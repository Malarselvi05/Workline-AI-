# apps/web environment variables
# Copy this to .env.local and fill in the values

# FastAPI backend URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket server URL (same host as backend)
NEXT_PUBLIC_WS_URL=ws://localhost:8000
